const readline = require('readline')
const reader = readline.createInterface({
    input: process.stdin,
    output: process.stdout
})
// Create board
// Create Player
// Create Game
// Move func
// Valid winner
        // valid winner moves comb
// End Game(over or player winner)